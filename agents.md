# Repository agent instructions

Read and obey the canonical `ORESoftware/.github` lowercase `agents.md` before
editing. Preserve the credential-free local-transport boundary. Tests must use
self-created temporary directories and must prove that an isolated Git consumer
can clone the produced bare repository.
